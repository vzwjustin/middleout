"""Benchmark package for middleout-proxy similarity primitives.

Importable as ``python -m benchmarks.bench_similarity``. Each script under this
package self-bootstraps ``src/`` onto ``sys.path`` so it also runs standalone
via ``python benchmarks/bench_similarity.py``.
"""
